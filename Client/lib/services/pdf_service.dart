import 'dart:async';

/// PDF generation stubs. Replace with real implementation.
class PdfService {
  static Future<void> exportAttendancePdf({required DateTime? start, required DateTime? end}) async {
    await Future.delayed(const Duration(milliseconds: 400));
    // Placeholder: integrate with printing/pdf package later.
  }

  static Future<void> exportSalaryPdf({required DateTime? start, required DateTime? end}) async {
    await Future.delayed(const Duration(milliseconds: 400));
  }
}
